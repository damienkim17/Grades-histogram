# Grades-histogram
- Allows users to view histogram generated by uploadable .csv file with student names and grade scores
- Uses HTML5 Canvas API for generated histogram
- Displays statistics of highest, lowest, mean, and median scores
- Allows user defined input for specifying lower bounds for grades
